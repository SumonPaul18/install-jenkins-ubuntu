#
## Installation and Configuration Jenkins on Ubuntu 22.04.x: 
#
#### 
Copy these all command and paste & run in your  Ubuntu Server then after some time you can seen successfully Installed Jenkins in Server.
####
    git clone https://github.com/SumonPaul18/install-jenkins-ubuntu.git
    chmod +x -R install-jenkins-ubuntu/install-jenkins-ubuntu.sh
    . install-jenkins-ubuntu/install-jenkins-ubuntu.sh
    systemctl status jenkins
#### 
